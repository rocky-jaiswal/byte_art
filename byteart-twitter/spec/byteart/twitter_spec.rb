require "spec_helper"

describe Byteart::Twitter do
  it "has a version number" do
    expect(Byteart::Twitter::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
